# Include runtime dependencies so dev installs are complete.
-r requirements.txt

# Linting & formatting
ruff
black

# Typing
mypy
types-requests

# Testing
pytest
pytest-cov

# Security scanning
pip-audit
bandit

# Git hooks (optional but recommended)
pre-commit
