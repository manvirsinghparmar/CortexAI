// Copy this file to runtime-config.js and adjust values per environment.
// Include it before app.js in index.html when running frontend as a standalone component.
window.CORTEX_RUNTIME_CONFIG = {
    apiBase: "https://kudlo.triobrain.com/",
};
